/* To run 'prod' environment, run 'ng serve --environment=prod' or 'ng serve --prod' */

export const environment = {
	production: true
};

/**
 * For Production
 */
/* NOTE: Don't use 'https://'...just use 'http://' */
export const API_URL = 'http://lst.dev.clientreviews.info/api/v1/';
export const APP_URL = 'http://lst.dev.clientreviews.info/';
